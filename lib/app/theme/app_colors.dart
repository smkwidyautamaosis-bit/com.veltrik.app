import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const bgPrimary = Color(0xFFFFFFFF);   // White
  static const bgSurface = Color(0xFFF5F7FA);   // Light Gray
  static const bgCard    = Color(0xFFFFFFFF);   // White Card
  static const bgElevated = Color(0xFFEFF4FF);  // Soft Blue Tint

  // Accent
  static const accentBlue  = Color(0xFF2563EB); // Primary Blue
  static const accentRoyal = Color(0xFF1D4ED8); // Deeper Blue
  static const accentGlow  = Color(0xFF93C5FD); // Light Blue

  // Text
  static const textPrimary = Color(0xFF1E293B); // Near Black
  static const textSecond  = Color(0xFF64748B); // Slate Gray
  static const textMuted   = Color(0xFF94A3B8); // Light Slate

  // Semantic
  static const success = Color(0xFF10B981); // Emerald
  static const warning = Color(0xFFF59E0B); // Amber
  static const danger  = Color(0xFFEF4444); // Red
  static const info    = Color(0xFF2563EB); // Blue

  // Borders
  static const border      = Color(0xFFE2E8F0); // Light Border
  static const borderLight = Color(0xFFCBD5E1); // Slightly Darker

  // Shapes
  static const shapeAccent = Color(0x142563EB); // Blue 8% opacity
}
